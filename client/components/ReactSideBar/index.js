import React from 'react'
import ReactSideBarComponent from './ReactSideBar'

export default class ReactSideBar extends React.Component {
  render(){
    return(
      <ReactSideBarComponent sidebarOpen={true}/>
    )
  }
}
